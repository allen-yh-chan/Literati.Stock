# signal-notify-discord

Discord webhook notification for volume_surge_red signal events. NotificationChannel Protocol + DiscordWebhookChannel + daily dispatch job at 17:50 Taipei that reads signal_event for today and posts a single embed if events exist (skip on empty).
