# price-domain

Derived stock_price domain table populated by an ELT transform job reading ingest_raw JSONB payloads, with an ingest_cursor tracking table for idempotent replay. Scope: TaiwanStockPrice only.
