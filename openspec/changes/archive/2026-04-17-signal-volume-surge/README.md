# signal-volume-surge

First signal (volume_surge_red) + signal engine foundation: Signal Protocol, SignalEvent table, SignalEvaluationService with MA window feature computation in SQL, scheduled daily evaluation at 17:45 Taipei, literati-signal CLI with evaluate / backfill subcommands.
